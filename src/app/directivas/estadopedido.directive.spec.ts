import { EstadopedidoDirective } from './estadopedido.directive';

describe('EstadopedidoDirective', () => {
  it('should create an instance', () => {
    const directive = new EstadopedidoDirective();
    expect(directive).toBeTruthy();
  });
});
