import { TemadarkDirective } from './temadark.directive';

describe('TemadarkDirective', () => {
  it('should create an instance', () => {
    const directive = new TemadarkDirective();
    expect(directive).toBeTruthy();
  });
});
