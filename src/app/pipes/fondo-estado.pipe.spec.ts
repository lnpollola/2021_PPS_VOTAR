import { FondoEstadoPipe } from './fondo-estado.pipe';

describe('FondoEstadoPipe', () => {
  it('create an instance', () => {
    const pipe = new FondoEstadoPipe();
    expect(pipe).toBeTruthy();
  });
});
